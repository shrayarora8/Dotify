<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="12dp"
    android:foreground="?selectableItemBackground">

    <ImageView
        android:id="@+id/ivSongArt"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintLeft_toLeftOf="parent" />

    <TextView
        android:id="@+id/tvNameOfSong"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="NAME"
        android:layout_marginLeft="16dp"
        android:textSize="24sp"
        app:layout_constraintLeft_toRightOf="@id/ivSongArt"
        app:layout_constraintTop_toTopOf="@id/ivSongArt"
        />

    <TextView
        android:id="@+id/tvArtistName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Name of Artist"
        app:layout_constraintLeft_toRightOf="@id/ivSongArt"
        app:layout_constraintTop_toBottomOf="@id/tvNameOfSong"
        app:layout_constraintBottom_toBottomOf="@id/ivSongArt"
        android:layout_marginLeft="16dp"
        android:textSize="24sp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>